# API base URL — must match backend PORT in ../backend/.env (default 4100).
# Example: if backend/.env has PORT=4100, use:
NEXT_PUBLIC_API_URL=http://localhost:4100

# CORS: backend/.env FRONTEND_URL should be http://localhost:3000 when using `next dev`.
